# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Packer -- build.log is written by the custom-image workflow
build.log

# Cosign / key material -- no key is committed, public halves included.
# The KMS public key is exported at deploy time by .github/workflows/deploy.yml.
*.key
cosign.key
*.pem
*.pub

# Editor / OS
.DS_Store
*.swp
*~
